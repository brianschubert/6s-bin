name: Download and cache

on:
  workflow_call:
    inputs:
      urls:
        required: true
        type: string
      name:
        required: true
        type: string
      path:
        required: true
        type: string
      artifact:
        required: true
        type: string


jobs:
  download:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        url: ${{ fromJSON(github.event.inputs.urls) }}
    steps:
      - name: Download file
        run:
          curl --create-dirs --output-dir ${{ github.event.inputs.path }}s -O ${{ matrix.url }}
      - uses: actions/upload-artifact@v3
        with:
          name: ${{ github.event.inputs.name }}
          path: ${{ github.event.inputs.path }}
          retention-days: 1