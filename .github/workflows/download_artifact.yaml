name: Download and cache

on:
  workflow_call:
    inputs:
      urls:
        required: true
        type: string
      artifact-path:
        required: true
        type: string
      artifact-name:
        required: true
        type: string


jobs:
  download:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        url: ${{ fromJSON(github.event.inputs.urls) }}
    steps:
      - name: Download file
        run:
          curl --create-dirs --output-dir ${{ github.event.inputs.artifact-path }}s -O ${{ matrix.url }}
      - uses: actions/upload-artifact@v3
        with:
          name: ${{ github.event.inputs.artifact-name }}
          path: ${{ github.event.inputs.artifact-path }}
          retention-days: 1